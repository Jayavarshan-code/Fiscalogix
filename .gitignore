# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
.venv/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Project specific
backend/tmp_uploads/

# Secrets — never commit these
.env
backend/.env
*.env.local

# Frontend env — .env.production IS committed (only public VITE_ vars)
# backend/.env is NOT committed (contains API keys)

# ML model binaries — auto-trained on first boot, no need to version
*.pkl
*.pt

# Synthetic data — regenerated by generator.py
backend/synthetic_*.csv
